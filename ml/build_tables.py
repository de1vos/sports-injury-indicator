"""
Phase 2 — Build clean CSV tables from raw API JSON.

Steps:
  2.1  players.csv          — one row per unique player
  2.2  season_stats.csv     — one row per player per season
  2.3  match_stats.csv      — one row per player per match (2025/26)
  2.4  injuries.csv         — one row per injury/sidelined event

Run after collect_players.py (and other collect scripts) have finished.
"""

# TODO: implement after data collection is validated

if __name__ == "__main__":
    print("Phase 2 not yet implemented.")
